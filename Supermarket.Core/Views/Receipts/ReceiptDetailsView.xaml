<Window x:Class="Supermarket.Core.Views.Receipts.ReceiptDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Supermarket.Core.Views.Receipts"
        mc:Ignorable="d"
        Title="ReceiptDetailsView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Receipt Details" FontSize="20" Margin="0,0,0,20" TextAlignment="Center" />
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Issued At: " Margin="0,0,0,5" />
                <TextBlock Text="{Binding ReceiptDto.IssuedAt}" Margin="0,0,0,10" />
                <TextBlock Text="Issuer: " Margin="0,0,0,5" />
                <TextBlock Text="{Binding ReceiptDto.Issuer.Name}" Margin="0,0,0,10" />
                <TextBlock Text="Total: " Margin="0,0,0,5" />
                <TextBlock Text="{Binding ReceiptDto.Total}" Margin="0,0,0,10" />
                <Button Content="Back" Command="{Binding BackCommand}" Width="100" HorizontalAlignment="Center" Margin="10" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
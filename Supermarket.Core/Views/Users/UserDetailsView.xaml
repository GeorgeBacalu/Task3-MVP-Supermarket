<Window x:Class="Supermarket.Core.Views.Users.UserDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="UserDetailsView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="User Details" FontSize="20" Margin="0,0,0,20" TextAlignment="Center" />
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Name: " Margin="0,0,0,5" />
                <TextBlock Text="{Binding UserDto.Name}" Margin="0,0,0,10" />
                <TextBlock Text="Email: " Margin="0,0,0,5" />
                <TextBlock Text="{Binding UserDto.Email}" Margin="0,0,0,10" />
                <TextBlock Text="Role: " Margin="0,0,0,5" />
                <TextBlock Text="{Binding UserDto.Role.Type, Converter={StaticResource RoleConverter}}" Margin="0,0,0,10" />
                <TextBlock Text="Created At: " Margin="0,0,0,5" />
                <TextBlock Text="{Binding UserDto.CreatedAt}" Margin="0,0,0,10" />
                <Button Content="Back" Command="{Binding BackCommand}" Width="100" HorizontalAlignment="Center" Margin="10" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>